TR = 2.5;
TA = 2.5;

IS_CLINICAL = false;

NUM_IMG_STAB = struct('sessName', {'se_fingerfootlips'},'nBeg', {0});
NUM_IMG_TRUNC = struct('sessName', {'se_fingerfootlips'},'nEnd',{0});

FIS_UNITS = 'secs';

FIS_CONDITIONS{1} = struct(...
    'sessName', 'se_fingerfootlips',...
    'condnames', {'finger'},...
    'onsets', {10:90:430},...
    'duration',{15});

GU_CONTRAST = { struct(...
    'contrastName', 'cn_figer', ...
    'name', 'Motor finger: group activations', ...
    'coninfo', 'co_finger', ...
    'negative', true, ...
    'nameNegative', 'Motor finger: deactivations',...
    'sessrep', 'none'),...
};